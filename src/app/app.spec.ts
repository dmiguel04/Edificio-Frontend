// Placeholder Jasmine test so tsconfig.spec.json include pattern finds at least one spec.
// Uses Jasmine globals (describe, it, expect) provided by Angular test runner configuration.
describe('placeholder', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
